export abstract class MathObject {
    // abstract parse(string): this is MathObject;

    //abstract parse2(string): this;

    //static parse3<TMathObject extends MathObject>(input): TMathObject;

    //abstract parse3(string): typeof MathObject;
    
    abstract toString(): string;
}