export enum Sign {
    Positive = '',
    Negative = '-'
}

export enum Operators {
    None = '',
    Addition = '+',
    Subtraction = '-',
    Division = '/',
    Multiplication = '*'
}

export enum TrigTypes {
    sin = 'sin',
    cos = 'cos',
    tan = 'tan',
    sec = 'sec',
    csc = 'csc',
    cot = 'cot'
}

export enum LogTypes {
    log = 'log',
    ln = 'ln'
}

export enum Constants {
    E = 'E',
    PI = 'PI'
}
